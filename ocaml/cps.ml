(*
  example of continuation passing style to achieve a tail recursive equivalent to a recursive function 
  CPS is also utilized within compiler optimizations, as some compilers will automatically optimize
  for this.

  
*)
